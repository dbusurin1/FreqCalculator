# API Endpoints
# API base URL для всех запросов к платформе Creao
VITE_API_BASE_PATH=https://api.creao.ru

# MCP API base URL для MCP tool вызовов
VITE_MCP_API_BASE_PATH=https://api.creao.ru

# Tenant Configuration
# ID организации/тенанта (опционально, для мультитенантности)
VITE_TENANT_ID=

# Data Schema Flag
# Флаг необходимости схемы данных (используется для ORM)
NEEDS_DATA_SCHEMA=true

# Development Settings (опционально)
# NODE_ENV=development
# VITE_DEV_PORT=3000
