# Калькулятор Эффективной Частоты Рекламы

Современное веб-приложение для расчёта оптимальной частоты рекламных контактов с использованием ИИ-анализа.

## 🚀 Быстрый старт

### Установка зависимостей

```bash
npm install
```

### Локальный запуск (Development)

⚠️ **ВАЖНО:** Приложение использует Vite, поэтому **НЕ открывайте `index.html` напрямую в браузере!**

Это вызовет CORS ошибки:
```
Access to script at 'file:///C:/src/main.tsx' from origin 'null' has been blocked by CORS policy
```

**Правильный способ запуска:**

```bash
npm run dev
```

Это запустит dev-сервер на `http://localhost:3000`

### Production сборка

```bash
npm run build
```

Собранные файлы будут в директории `dist/`

### Просмотр production сборки

```bash
npm run serve
```

## 📦 Основные команды

| Команда | Описание |
|---------|----------|
| `npm run dev` | Запуск dev-сервера (порт 3000) |
| `npm run build` | Production сборка с проверкой типов |
| `npm run serve` | Просмотр production сборки |
| `npm run check:safe` | TypeScript проверка + ESLint (с таймаутом 20 сек) |

## 🔧 Настройка переменных окружения

Создайте файл `.env` в корне проекта:

```env
# API endpoints
VITE_API_BASE_PATH=https://api.creao.ru
VITE_MCP_API_BASE_PATH=https://api.creao.ru

# Tenant (опционально)
VITE_TENANT_ID=
```

См. `.env.example` для полного списка переменных.

## 🎯 Функциональность

- **ИИ-Анализ брендов** - автоматическое определение параметров на основе названия бренда
- **Расчёт эффективной частоты** - оптимальное количество рекламных контактов
- **Прогноз КПИ** - охват ЦА, рост Top of Mind, LTV Growth
- **История расчётов** - автоматическое сохранение для авторизированных пользователей
- **Мультиязычный интерфейс** - полностью на русском языке

## 🛠 Технологический стек

- **React 19** - UI библиотека
- **TypeScript** - типизация
- **TanStack Router** - маршрутизация
- **TanStack Query** - управление серверным состоянием
- **Tailwind CSS v4** - стилизация
- **shadcn/ui** - компоненты UI
- **Zustand** - state management
- **Vite** - сборщик и dev-сервер

## 📋 Решение проблем

### CORS ошибки при открытии index.html

**Проблема:**
```
Access to script at 'file:///...' from origin 'null' has been blocked by CORS policy
```

**Решение:**
Используйте `npm run dev` вместо прямого открытия HTML файла.

### ИИ-анализ не работает

**Причина:** Требуется авторизация в системе Creao или настройка переменных окружения.

**Решение:**
1. Убедитесь, что `.env` файл создан и содержит корректные API endpoints
2. Для локальной разработки можно использовать приложение без авторизации - ИИ-анализ будет выдавать сообщение об ошибке, но ручная настройка параметров работает полностью

### Module not found

**Решение:**
```bash
rm -rf node_modules package-lock.json
npm install
```

## 📁 Структура проекта

```
vite-template/
├── src/
│   ├── routes/           # Страницы приложения
│   │   └── index.tsx     # Главная страница с калькулятором
│   ├── components/       # React компоненты
│   │   ├── ui/          # shadcn/ui компоненты
│   │   └── data/        # ORM и data layer
│   ├── hooks/           # Custom React hooks
│   ├── sdk/             # Creao platform SDK
│   └── styles.css       # Глобальные стили
├── index.html           # HTML точка входа
├── package.json         # Зависимости
└── vite.config.ts       # Конфигурация Vite
```

## 🤝 Разработка

### Правила кода

- TypeScript обязателен
- Используйте `@/` алиас для импортов
- Следуйте конвенциям shadcn/ui
- Icons только из `lucide-react`

### Code Style

Код автоматически форматируется с помощью Biome:
```bash
npm run format
```

## 📄 Лицензия

MIT

## 💬 Поддержка

При возникновении проблем создайте issue в репозитории проекта.
